for (int i = 0; i < N; i++) {
    found = true 
    for (int j = i; j < i + n; j++) {
        if(j >= N or a[i] != b[j]) found false break
    }
    if (found)
        print(foudn)
        exit
}
print(not found) exit
